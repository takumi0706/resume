# Design — 小山田卓生 ポートフォリオ

ロック済みのデザインシステム。以降の Hallmark 実行はこのファイルを最初に読み、
各ページはこれに従う。**ページ間で見た目が変わってはならない** — 多様化ルールは
このファイルの存在によって反転し、「ページはシステムを共有する」が正となる。

変更は意図的に。このファイルが規則そのもの。

## System

- Genre · modern-minimal
- Theme · custom（vibe: 「工学的な正確さ、寒色、無装飾」）
- Axes · light / geometric-sans / neutral
- Nav · N3 Side-rail（全ページ共通）
- Footer · Ft2 Inline single line（全ページ共通）

**nav と footer はサイト共通のクロームなので、ページごとにローテーションさせない。**
同一サイトの別ページでナビが変わるのは UX の破綻であり、多様化ルールの適用対象外。

## Page shapes

マクロ構造だけはページの役割に応じて変える。トークン・書体・モーション・CTA の声は共有する。

| ページ | マクロ構造 | 理由 |
|---|---|---|
| `/` | 03 Marquee Hero | 名前と読みを看板として置く |
| `/resume/` | 02 Long Document | 職務経歴書は読み物。連続する散文と内在する見出し |
| `/oss/` | 04 Stat-Led | 主役が1つ（google-calendar-mcp）で階層が明確。数字が narrative |
| `/community/` | 12 Letter | 一人称。運営に関わる理由は本人の言葉でしか書けない |
| `/skill/` | 01 Bento Grid | 数の多い小さなもの。タイルの大小で実績の厚みを表す |
| `/blog/` | 13 Index-First | ページ自体が目録。リンクがボタンを兼ねる |

新しいページを足すときは、既出のマクロ構造と重複しないものを選び、この表に記録する。

**選定時は「Reach for it」だけでなく「Avoid」を必ず読むこと。** 実際に
以下の候補が Avoid 節で弾かれた。

| 候補 | 弾かれた理由 |
|---|---|
| 11 Catalogue → `/oss/` | 「全項目を等価に扱う。階層のあるものには誤り」 |
| 01 Bento Grid → `/oss/` | 「8〜15ブロックが前提」「単一の主張のページには使うな」 |
| 14 Narrative Workflow → `/community/` | 「明示的なワークフロー向け。実在の順序が必要」 |
| 15 Split Studio → `/community/` | 対向半分に視覚的な証拠が要る（画像がない）。しかも Reference が Vercel |
| 06 Conversational FAQ → `/community/` | 「主たるページには使うな。開幕を担う別のマクロ構造と組む前提」 |

## マクロ構造の既定から外している点

| 外したもの | 理由 |
|---|---|
| Stat-Led の number-tick（0からのカウントアップ） | motion-cut のため |
| Split Studio / Bento のリビール | 同上 |
| Letter の「セリフ体イタリックの呼びかけ」 | 見出しのイタリックは全体で禁止（gate 38a）。かつ modern-minimal はサンセリフ通し |
| Bento のヒーロー中央揃え | 中央揃えの連続は anti-pattern（Centred everything）。サイト全体も左寄せ |
| Marquee Hero の「サブヘッドを置かない」 | 個人サイトの看板として名前と読みの2行を出す |
| ヒーローの gate 44a（下パディング1.3倍） | 直後に太罫があり、罫線が次セクションへの引き込みを担っている |

## 和文フォントの先読み

`Base.astro` の `preloadDisplayWeight` で、和文の太いウェイト(600)を先読みするか
ページ側が決める。既定は true。

見出しに和文を持たないページ（`/blog/` など）で先読みすると 41KB を落として
捨てることになり、ブラウザが「preload したのに使われていない」と警告を出す。
そういうページは `preloadDisplayWeight={false}` を渡す。

## 連絡先の出し分け

**一律にしない。相手が居る場所が違う。** 定義は `src/config/site.ts`。

| 場所 | 窓口 | 理由 |
|---|---|---|
| `/resume/` の連絡先・ナビ CTA | `formalContact`（LinkedIn） | 採用担当は LinkedIn に常時ログインしている。正式な打診に向く |
| `/community/` の締め | `communityContact`（X） | カンファレンス運営者は LinkedIn に居ない。ここを LinkedIn にすると誰からも声がかからない |
| フッター | `elsewhere` に全部並べる | 目次なので網羅する |

**LinkedIn はログアウト状態だと認証壁（`/authwall`）に飛ぶ。** 採用担当には影響
しないが、それ以外の読者には壁になる。だから一律にしてはいけない。

URL は必ず `src/config/site.ts` から参照する。ページに直書きしない。

## 情報密度 — 1行1事実

**同じ内容を散文で書くと、読まないと分からなくなる。** 事実は行データとして置く。

参考にした shunsock/resume を同じ物差しで測ると、職務経歴書は
「散文 212字 / 箇条書き 69項目 = 3」。こちらは当初「458字 / 15行 = 31」で、
**10倍読ませていた。**

新しいページを足すとき、あるいは既存のページを触ったときは以下を確認する。

- 経歴・実績・スペックは `Facts` コンポーネント（定義リスト）か表で置く
- 散文は「なぜそうしたか」「何を考えたか」にだけ使う。事実の記述に使わない
- **記録は散文より先に置く。** 読み手が実績にたどり着くまでに文章を挟まない
- 目安として、1ページの散文字数 ÷ 行データ数が 30 を超えたら文章に寄りすぎ

以下で測れる（順序は測れないので、目視も併せる）。

```
node -e 'const fs=require("fs");const h=fs.readFileSync("dist/index.html","utf8");
const m=(h.match(/<main[\s\S]*?<\/main>/)||[""])[0];
const p=[...m.matchAll(/<p\b[^>]*>([\s\S]*?)<\/p>/g)].map(x=>x[1].replace(/<[^>]*>/g,"").trim())
  .filter(t=>t.length>40).reduce((s,t)=>s+t.length,0);
const r=(m.match(/<tr\b/g)||[]).length+(m.match(/<li\b/g)||[]).length+(m.match(/<dt\b/g)||[]).length;
console.log(p,"字 /",r,"行 =",(p/r).toFixed(0));'
```

## 看板に思想を書かない

トップの見出しで「その人がどういう考え方をするか」を一文にしようとして2回転けた
（「万象を理解し、問題を解決する〜」「利他主義の〜」）。

- 抽象的な標語は、書いた本人以外には伝わりにくい
- 自称の徳目（利他主義・誠実・情熱）は、裏付けが下に無いと逆効果になる
- 思想は `/skill/` や `/community/` の本文で、具体を伴って語る

看板は「誰か」「何をやっている人か」を出す。参照した shunsock/resume も
名前・読み・ハンドルだった。ただしこちらはレールにハンドルが常時出ているので、
看板では名前と読みだけにしている。

## 設計の説明を読者に読ませない

**見れば分かることを文章で説明しない。** 書いた側の都合であって、読者には不要。

実際に出してしまった例:

- 「タイルの大きさは実績の件数です」 — タイルを見れば分かる
- 「なので『中級』『実務レベル』といった段階は書きません」 — 書いていないことを
  わざわざ宣言している
- CTA の上の「〜は職務経歴書にまとめています」 — ボタンに「職務経歴書を読む」と
  書いてある。同じことを2回言っている

判断の基準:

- その文を消して、読者が困るか。困らないなら消す
- 「なぜこう作ったか」はコードのコメントか design.md に書く。ページには出さない
- ページに載せてよい散文は、**本人にしか書けないこと**だけ（経緯・動機・実際にやったこと）

## 和文に欧文のスケールを当てない

**Hallmark の文字数ベースの規定は欧文前提。** 和文は字幅が約2倍あるため、
「欧文換算◯字だからこのサイズ」という計算をそのまま当てると過大になる。

- 見出しは `--text-display`（最大84px）ではなく **`--text-display-ja`（最大44px）** を使う
- サイドレールのラベルは短くする。`src/i18n/ui.ts` の `navShort` を使う。
  和文4字を超えると折り返し、gate 49（クリック可能テキストの2行禁止）に触れる
- 幅の指定に `ch` を使わない。`ch` は半角基準なので和文では2倍必要になる

## 文字サイズの下限

`--text-xs` (10.24px) は使わない。規約の絶対下限ぎりぎりで実用に耐えない。
ラベルの最小は `--text-sm` (12.8px)。

## 文字サイズの上限 — display の register を持ち込まない

トップの氏名は 84px → 54px → **36px**（`--text-display-s`、上限 2.25rem）と
3回下げている。毎回「まだデカい」と言われたのは、値の詰め方ではなく
**書体の register を取り違えていた**ため。

大きな字＋強いトラッキング（`-0.03em`）は「主張を1行で言い切る」着地ページの
ためのもので、事実を並べた索引の上に置くものではない。

- 本文 16px に対して 36px あれば、タイトルであることは誤解の余地なく伝わる
- 最初の画面は看板ではなく事実（現職 / 担当 / 技術 / いま）で埋める
- 氏名はレールのワードマーク・欧文・和文で最初の画面に3回出ている。
  3回目を一番大きく出す理由はない
- 欧文と和文を2行で積むときは 36:20 程度まで差をつける。
  36:25 では2行が競い、見出しが2つあるように読める
- `--text-display`（最大84px）を使ってよいのは `/oss/` の主役の数字だけ

## import のルール

相対パス（`../`）は使わない。`tsconfig.json` の `paths` で定義したエイリアスを使う。

| エイリアス | 指す先 |
|---|---|
| `@/*` | `src/*` |
| `@layouts/*` | `src/layouts/*` |
| `@components/*` | `src/components/*` |
| `@styles/*` | `src/styles/*` |
| `@i18n/*` | `src/i18n/*` |
| `@views/*` | `src/page-views/*` |

## サイドレール

行き先が6つあるのにナビにリンクが1本も無い状態だった（N9 Edge-aligned minimal）。
訪問者はフッターまでスクロールしないと他のページの存在に気づけなかったため、
N3 Side-rail に変えた。

- レールは `.page` グリッドの全高に伸ばす。`align-items: start` にすると
  中身の高さ（100dvh）しか無くなり、そこを通り過ぎた時点で sticky が切れる
- レールと本文はひとまとまりで中央に置く。本文だけを中央寄せにすると
  レールとの間に隙間が空き、左詰めにすると右側に大きな空白が残る
- ラベルは `src/i18n/ui.ts` の `navShort` を使う。和文4字を超えるとレール幅で
  折り返し、gate 49（クリック可能テキストの2行禁止）に触れる。
  `src/config/site.ts` の `sections` はキーとパスだけを持ち、ラベルを持たない
- 現在地は色ではなく帯で示す。色だけだと見分けがつきにくい

## ページを閉じる線は1本だけ

レールは `"rail main"` の行までで終わらせ、フッターは `"footer footer"` で
全幅に伸ばす。

以前はレールを `"rail footer"` でページ最下部まで貫かせていた。すると sticky の
解除位置がページ末尾になり、レール下端の「連絡する」がフッターの帯の内側に
落ちてくる。閉じの罫線がレール幅と本文幅で2本並び、高さも揃わなかった
（罫線で31px、文字で16pxのずれ）。

- 閉じる線は全幅の1本。レール側も同時に終端されるので、罫線を2本引く必要がない
- `.site-footer .shell` だけ `max-inline-size: none`。本文幅に閉じ込めると
  レール幅ぶん左に空白が残り、帯が本文カラムにしか掛かっていないように見える
- `.rail__contact` には罫線を引かない
- 結果、「連絡する」とフッターのリンクと罫線が同じ `--page-gutter` から始まる
- sticky はフッターの手前で解除される。スクロール中の固定表示はそのまま

## アイコンをテキストと並べる

`base.css` のリセットが `img, picture, svg { display: block }` を当てている。
単独の画像には正しいが、テキストと並べるアイコンには誤りで、
打ち消さないとアイコンが自分の行を占有してラベルの上に乗る。
`BrandIcon` 側で `display: inline-block` にして打ち消している。

- アイコンは常にラベルの前に置き、`margin-inline-end: 0.4em` を空ける。
  詰めると X は「𝕏 X」、Zenn は「//Zenn」と地続きに読める
- 下線はラベルにだけ引く。リンク全体に引くとアイコンの下にも線が走る
- 区切りの中黒はリンクの外側（`li`/`span` の `::before`）に置く。
  リンクの `::before` にすると下線が引かれ、クリック領域にも入る
- リンクを `inline-flex` にしない。ベースラインの基準が最初のフレックス要素に
  なるため、アイコンを持つリンクと持たないリンクで並びがズレる

## Tokens

`src/styles/tokens.css` が唯一の情報源。以下は要約。

```css
:root {
  /* モノクロ厳密。有彩色のアクセントを持たず、アクセントの役割はインク自身が担う。
   * 唯一の有彩色は --color-focus で :focus-visible にのみ現れる。 */
  --color-paper:      oklch(99% 0.005 240);
  --color-paper-2:    oklch(96.5% 0.006 240);
  --color-paper-3:    oklch(93.5% 0.007 240);
  --color-ink:        oklch(19% 0.013 245);
  --color-ink-2:      oklch(44% 0.012 245);
  --color-muted:      oklch(47% 0.011 243);   /* 4.5:1 を満たす明るさの上限付近 */
  --color-rule:       oklch(82% 0.008 240);
  --color-rule-2:     oklch(88.5% 0.007 240);
  --color-accent:     var(--color-ink);
  --color-accent-ink: var(--color-paper);
  --color-focus:      oklch(55% 0.19 250);

  /* 和欧混植: 欧文は Geist、和文は IBM Plex Sans JP がグリフ単位で分担する。
   * ファミリー数は 3（2+1 ルールの上限）。これ以上増やさない。 */
  --font-display: "Geist Variable", "IBM Plex Sans JP", ui-sans-serif, system-ui, sans-serif;
  --font-body:    "Geist Variable", "IBM Plex Sans JP", ui-sans-serif, system-ui, sans-serif;
  --font-outlier: "Geist Mono Variable", "IBM Plex Sans JP", ui-monospace, monospace;

  /* 4pt スケール --space-3xs … --space-3xl · 型スケールは 1.25（長三度） */
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --dur-short: 120ms;  --dur-base: 200ms;
  --radius-sm: 4px;  --radius-md: 6px;   /* ピル型は使わない */
}
```

ダークモードはトークンの差し替えのみで実現する。構造・タイポグラフィ・余白は変えない。

## 和文組版

このサイトの差別化の核。`src/styles/base.css` の `@layer japanese` を参照。

- `text-spacing-trim: trim-start` — 約物のアキ詰め
- `text-autospace: normal` — 和欧間の四分アキ
- `line-break: strict` — 行頭に句読点・閉じ括弧を置かない
- `--measure-ja: 34em` — 行長は全角34字。`ch` は半角基準なので和文には使えない
- `--leading-body: 1.85` — 欧文向けの 1.5〜1.65 では和文は詰まる
- ラベルに `text-transform: uppercase` を使わない（和文に作用しない）

## outlier の割り当て

Geist Mono は **2スロットのみ**（2+1 ルール / gate 38）。

1. ワードマーク（ヘッダー・フッター）
2. 表の年号列（等幅数字）

3つ目に使いたくなったら、それは本文書体に戻すべきもの。

## CTA voice

- Primary · `--color-accent` の塗り · `--radius-md`(6px) の**矩形** · `--space-xs var(--space-md)`
  - ピル型（`border-radius: 999px`）は使わない。Vercel / Linear を最も強く想起させる形のため
- Secondary · 下線付きのテキストリンク · 矢印は本文書体のまま

## Motion stance

motion-cut。プリミティブは2つまで。

- リンクの下線が太くなる
- ボタンの塗りが変わる + 1px の押し込み

スクロール連動のリビールは使わない。`prefers-reduced-motion: reduce` では
transform を無効化し、色の変化のみに落とす。

## 守るべき最低ライン

- 色と `font-family` は必ず名前付きトークン経由。インラインの hex / oklch / 直書き禁止
- 見出しに italic を使わない
- コントラストは本文 4.5:1 / 大きい文字・アイコン・フォーカスリング 3:1
- 320 / 375 / 414 / 768px で横スクロールを出さない
- クリック可能なテキストを2行にしない
- **リンクの下線に `--color-rule` を使わない。** 罫線は目立たなくてよいが、
  リンクの手がかりは目立たないと機能しない。`--color-rule` はダークモードで
  背景と 1.49:1 しかなく、リンクが本文と区別できなくなっていた。
  リンクの文字色は本文と同色なので、下線が唯一の手がかりになる。`--color-muted` を使う
- **実データのない数値・実績を書かない。** 事実は `src/content/` と
  `docs/specs/portfolio-site-spec.md` にあるものだけを使う
- **第三者の実名を本人の許諾なく書かない。** 「紹介された」「引き継いだ」は
  いずれも相手の行動であり、公開の場に書けば相手を巻き込む
- **他人の内心・動機を代弁しない。** なぜ引き継いだのか、なぜ辞めたのかは
  本人にしか語れない。書けるのは起きた事実まで
- **発注元のシステム構成を説明しない。** 自分の実績を語るつもりで、相手の
  内部事情を書いてしまう事故が起きやすい。実際に3回やった

## 業務経歴で書けること・書けないこと

「自分が何をしたか」と「相手のシステムがどうなっているか」は別物。
後者は自分の実績ではなく、相手の情報。

| | 例 |
|---|---|
| 書ける | 何を作ったか（「プッシュ通知の基盤を一式実装」） |
| 書ける | どう進めたか（「新旧を並走させ Feature Flag で切り替えた」） |
| 書ける | 使った言語・ツール（`stack` に書く粒度） |
| 書ける | 公開済みのプロダクト名・企業の公式発表にある情報 |
| **書けない** | 相手のリソース構成（「Lambda + DynamoDB + S3 で、リポジトリパターン」） |
| **書けない** | 相手の業務の実態（「既存は表計算で運用していた」） |
| **書けない** | 組織名・チケット ID・ドメイン固有の用語 |
| **書けない** | セキュリティ事案 |

判断に迷ったら、**その一文を相手の担当者が読んで嫌がるか**で考える。

会社名をリンクするときは URL を必ず開いて確認する。`nagase.co.jp` は
長瀬産業株式会社（化学商社）で、東進を運営する株式会社ナガセとは別法人。
社名が同じでもドメインが別のことがある。

## 「本人の申告」と「こちらの創作」を混同しない

止めるべきなのは **こちらが埋めた事実** であって、**本人が申告した事実** ではない。

TSKaigi の開催日を勝手に `2025-01-01` にしたのは創作で、これは事故。
一方「HonoConf の実行委員長」は本人の申告で、これは書いてよい。
公式サイトに肩書きの記載が無いことを理由に確認待ちにしたのは、基準の誤り。

- このサイトの事実はほとんどが自己申告。LINEヤフーの担当業務も、ナガセの
  Part Tech Lead も、公式ページで第三者が検証できるものではない。
  ひとつの経歴にだけ外部の裏付けを求めるのは一貫していない
- **推論を根拠にしない。** 「スタッフ一覧の先頭にいるから実行委員長」は成り立たない
  （表示順は五十音順かもしれない）。そもそも根拠は要らないので、使わないほうが強い
- `url` はイベントやプロダクトが実在することを示すためのもので、肩書きの証明ではない
- 分からないことは本人に聞く。聞いて「覚えていない」なら、書かずに省く
  （ドリコム・Kyoto Development Institute はこれで業務内容ごと落とした）

## i18n — 事実を複製しない

日本語が既定で接頭辞なし（`/resume/`）、英語は `/en/resume/`。
既に公開済みなので、日本語を `/ja/` に動かすと外部からのリンクが切れる。

置き場所の使い分け:

| 置き場所 | 内容 |
|---|---|
| `src/content/<集合>/<id>.md` | 事実（日付・URL・stack・star 数）＋日本語 |
| `src/content/en/<集合>/<id>.md` | 訳す必要のあるものだけ |
| `src/i18n/ui.ts` | ラベル・見出し・短い定型句 |
| `src/i18n/names.ts` | 会社名・学校名・会場名の対訳表 |
| `src/content/prose/` と `src/content/en/prose/` | 複数文の散文 |

- **事実は日本語側にしか置かない。** 言語ごとにファイルを丸ごと複製すると、
  日付を直したのに片方だけ古い、というずれが必ず起きる
- **日本語を値にしない。** `employment: 正社員` や `role: 実行委員長` のように
  閉じた集合は言語非依存のキーにする。表示名は辞書が持つ
- **散文を辞書に入れない。** 文を断片に割って言語ごとに組み立てると、語順が
  違う言語で必ず壊れる。`{name} からご連絡ください` は英語で崩れる
- **固有名詞は表に1つだけ。** 会社名は work と skills の usedIn の両方に出る。
  エントリごとに訳を持つと同じ会社が場所によって違う綴りになる。
  表に無いものはそのまま通す（Zenn の日本語記事タイトルは日本語のままが正しい）
- **公式の英語名しか書かない。** 各社・各校の公式サイトの表記を確認して入れる。
  公式の英語名が無い相手に `Inc.` を足して作らない
- ロケールは URL から引く（`splitLocale`）。実装は `src/page-views/` に1本だけ置き、
  `src/pages/` と `src/pages/en/` は薄いラッパーにする。CSS を二重に持たないため
- 英語ページでは和文フォントを読み込まない。使わない 100KB 超を捨てることになる

### 訳し漏れは機械で見る

訳が無いときは日本語にフォールバックする。壊れないのが利点だが、裏を返すと
**英語ページだけが日本語のまま出ても、日本語ページを見ている限り気づけない。**
`scripts/check-i18n.mjs` が CI で落とす。

- 英語ページに残ってよい和文は `ALLOW_JAPANESE` に理由付きで並べる
- Zenn の記事タイトルは `zenn.json` から機械的に集める。列挙すると記事が増えるたびに
  書き足すことになり、忘れた瞬間に検査が赤くなる
- `usedIn` の和文は、対訳表か Zenn の記事のどちらかに必ず存在していなければならない。
  この検査で「記事名が実際のタイトルと違う」ずれが2件見つかった

### glob の落とし穴

- `nagase.en.md` のような名前にすると、glob ローダーが id をスラッグ化して
  `nagase.en` が `nagaseen` に潰れる。読みにくいうえ衝突しうる
- 基底コレクション側の否定パターンで `!prose/**` を書くとマッチ全体が壊れた。
  ディレクトリを分ければ否定パターンそのものが要らない

## OGP カード

`scripts/build-og.mjs` が 1200×630 の PNG を `dist/og/` に焼く。
以前は `/icon.png`（正方形のアバター）に `twitter:card: summary` で、
X や Slack に貼っても誰の何のページか読めなかった。

- **文言はページ自身の `<title>` から取る。** ここに一覧を持つと、ページ名を
  変えたときにカードだけ古いまま残る
- **色は `tokens.css` を読んで OKLCH から sRGB に変換する。** 16進を焼き込まない。
  トークンを変えればカードも追随する
- 和文タイトルは 60px、欧文は 88px。同じ数値を当てない（→ 和文に欧文のスケールを
  当てない）。トラッキングも欧文の詰め（-0.03em）を和文に当てない
- 組版はサイトのヒーローと同じ。ハンドル → 主役 → 太罫 → 添え書き
- satori は woff2 を読めないので `subset-font` で sfnt に変換する。
  Geist は可変フォントなので `variationAxes` でウェイトをピン留めする
  （しないと既定インスタンスで焼かれ、太字が効かない）
- ファイル名の規則を `Base.astro` と `build-og.mjs` の2箇所で持っているため、
  `check-links.mjs` が `og:image` の実在を検査する。ずれると SNS 側でだけ
  画像が出なくなり、サイトを見ていても気づけない

## 載せないもの

- **入賞していないハッカソン。** `awards` は受賞の一覧で、参加歴の一覧ではない。
  運営でもないので `community` にも入らない。行き先が無いものは載せない

## Exports

`src/styles/tokens.css` が唯一の情報源。Tailwind v4 の `@theme`、DTCG の
`tokens.json`、shadcn/ui の CSS 変数が必要になったら
「design.md に Tailwind exports を追加して」と言えば Hallmark が追記する。
