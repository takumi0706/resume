---
summary: A feature added to a CLI that shows Git diffs the way GitHub does
---

difit is a CLI that starts a local server and shows the diff of a Git commit the way GitHub's Files changed view does.

I implemented support for writing GitHub-style suggestion blocks in code review comments.
