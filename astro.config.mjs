// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // OGP の絶対 URL 生成に使う。独自ドメインを取ったら差し替える。
  site: 'https://takumi0706.ganndamu0706.workers.dev',
});
